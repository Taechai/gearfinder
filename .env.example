# PostgreSQL credentials — choose any username and password
POSTGRES_USER=gearfinder
POSTGRES_PASSWORD=password123

# Used by Prisma/Next.js (automatically built from the above in docker-compose)
DATABASE_URL="postgresql://gearfinder:changeme@localhost:5432/gearfinderdb?schema=public"

# NextAuth — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="448a1fbe80c443ee9199e07b2fc18dced07f263c2ff1e505a3af9fcd8b9dfea0"
